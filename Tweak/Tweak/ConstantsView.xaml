<UserControl
    x:Class="Tweak.ConstantsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tweak"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="500"
    d:DesignWidth="400">

    <Grid>
        <StackPanel>
            <TextBlock Text="AIN1" />
            <TextBox Text="{Binding AIN1}" />
            <TextBlock Text="AIN2" />
            <TextBox Text="{Binding AIN2}" />
            <TextBlock Text="BIN1" />
            <TextBox Text="{Binding BIN1}" />
            <TextBlock Text="BIN2" />
            <TextBox Text="{Binding BIN2}" />
            <TextBlock Text="Wheel Radius" />
            <TextBox Text="{Binding WheelRadius}" InputScope="Digits" />
            <TextBlock Text="Line Width" />
            <TextBox Text="{Binding LineWidth}" />
            <TextBlock Text="Map Width" />
            <TextBox Text="{Binding MapWidth}" />
            <TextBlock Text="Map Height" />
            <TextBox Text="{Binding MapHeight}" />
            <TextBlock Text="Map Resolution" />
            <TextBox Text="{Binding MapResolution}" />
        </StackPanel>
    </Grid>
</UserControl>
