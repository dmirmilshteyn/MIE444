<UserControl x:Class="Tweak.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tweak"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <local:FilledToColorConverter x:Key="filledToColorConverter" />
    </UserControl.Resources>
    
    <Border BorderBrush="Black" BorderThickness="2">
        <ItemsControl ItemsSource="{Binding Tiles.Tiles}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="{Binding Tiles.Width}" Rows="{Binding Tiles.Height}" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Rectangle Width="5" Height="5" Fill="{Binding Filled, Converter={StaticResource filledToColorConverter}}" PreviewMouseMove="Rectangle_MouseEnter" MouseDown="Rectangle_MouseDown">
                        
                    </Rectangle>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Border>
</UserControl>
